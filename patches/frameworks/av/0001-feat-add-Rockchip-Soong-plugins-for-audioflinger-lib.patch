From adf8ea159e08afda6abe99b94d51c2ea2fa303e1 Mon Sep 17 00:00:00 2001
From: Vaciller <wyc_19533480830@outlook.com>
Date: Fri, 10 Jul 2026 14:51:45 +0800
Subject: [PATCH] feat: add Rockchip Soong plugins for audioflinger,
 libaudioclient, libaudiohal, audiopolicy

Change-Id: I876f3df1defb91ec12364b7ff1b70b4668396424

diff --git a/media/libaudioclient/Android.bp b/media/libaudioclient/Android.bp
index dccef0e88b..a97eb25119 100644
--- a/media/libaudioclient/Android.bp
+++ b/media/libaudioclient/Android.bp
@@ -1,3 +1,24 @@
+bootstrap_go_package {
+    name: "soong-libaudioclient",
+    pkgPath: "android/soong/libaudioclient",
+    deps: [
+        "blueprint",
+        "blueprint-pathtools",
+        "soong",
+        "soong-android",
+        "soong-cc",
+        "soong-genrule",
+    ],
+    srcs: [
+          "Android.go",
+    ],
+    pluginFor: ["soong_build"],
+}
+
+cc_libaudioclient {
+   name: "cc_libaudioclient_defaults",
+}
+
 package {
     // See: http://go/android-license-faq
     // A large-scale-change added 'default_applicable_licenses' to import
@@ -90,7 +111,7 @@ cc_library {
 
 cc_library {
     name: "libaudioclient",
-
+    defaults: ["cc_libaudioclient_defaults"],
     aidl: {
         export_aidl_headers: true,
         local_include_dirs: ["aidl"],
diff --git a/media/libaudioclient/Android.go b/media/libaudioclient/Android.go
new file mode 100755
index 0000000000..1f7aab1894
--- /dev/null
+++ b/media/libaudioclient/Android.go
@@ -0,0 +1,39 @@
+package libaudioclient
+
+import (
+        "android/soong/android"
+        "android/soong/cc"
+        "fmt"
+)
+
+func init() {
+    fmt.Println("libaudioclient want to conditional Compile")
+    android.RegisterModuleType("cc_libaudioclient", DefaultsFactory)
+}
+
+func DefaultsFactory() (android.Module) {
+    module := cc.DefaultsFactory()
+    android.AddLoadHook(module, Defaults)
+    return module
+}
+
+func Defaults(ctx android.LoadHookContext) {
+    type props struct {
+        Cflags []string
+        Shared_libs []string
+    }
+
+    p := &props{}
+    p.Cflags = getCflags(ctx)
+    ctx.AppendProperties(p)
+}
+
+func getCflags(ctx android.BaseContext) ([]string) {
+    var cppflags []string
+
+    if (ctx.AConfig().IsEnvTrue("BOARD_SUPPORT_MULTIAUDIO")) {
+        cppflags = append(cppflags,"-DSUPPORT_MULTIAUDIO=1")
+    }
+
+    return cppflags
+}
diff --git a/media/libaudiohal/impl/Android.bp b/media/libaudiohal/impl/Android.bp
index d30883a95c..e1aa311bdc 100644
--- a/media/libaudiohal/impl/Android.bp
+++ b/media/libaudiohal/impl/Android.bp
@@ -1,3 +1,24 @@
+bootstrap_go_package {
+    name: "soong-libaudiohal",
+    pkgPath: "android/soong/libaudiohal",
+    deps: [
+        "blueprint",
+        "blueprint-pathtools",
+        "soong",
+        "soong-android",
+        "soong-cc",
+        "soong-genrule",
+    ],
+    srcs: [
+          "Android.go",
+    ],
+    pluginFor: ["soong_build"],
+}
+
+cc_libaudiohal {
+   name: "cc_libaudiohal_defaults",
+}
+
 package {
     // See: http://go/android-license-faq
     // A large-scale-change added 'default_applicable_licenses' to import
@@ -9,6 +30,7 @@ package {
 
 filegroup {
     name: "audio_core_hal_client_sources",
+
     srcs: [
         "CoreConversionHelperHidl.cpp",
         "DeviceHalHidl.cpp",
@@ -29,7 +51,7 @@ filegroup {
 
 cc_defaults {
     name: "libaudiohal_default",
-
+    defaults: ["cc_libaudiohal_defaults"],
     cflags: [
         "-Wall",
         "-Wextra",
diff --git a/media/libaudiohal/impl/Android.go b/media/libaudiohal/impl/Android.go
new file mode 100755
index 0000000000..cff46d5e17
--- /dev/null
+++ b/media/libaudiohal/impl/Android.go
@@ -0,0 +1,39 @@
+package libaudiohal
+
+import (
+        "android/soong/android"
+        "android/soong/cc"
+        "fmt"
+)
+
+func init() {
+    fmt.Println("libaudiohal want to conditional Compile")
+    android.RegisterModuleType("cc_libaudiohal", DefaultsFactory)
+}
+
+func DefaultsFactory() (android.Module) {
+    module := cc.DefaultsFactory()
+    android.AddLoadHook(module, Defaults)
+    return module
+}
+
+func Defaults(ctx android.LoadHookContext) {
+    type props struct {
+        Cflags []string
+        Shared_libs []string
+    }
+
+    p := &props{}
+    p.Cflags = getCflags(ctx)
+    ctx.AppendProperties(p)
+}
+
+func getCflags(ctx android.BaseContext) ([]string) {
+    var cppflags []string
+
+    if (ctx.AConfig().IsEnvTrue("BOARD_SUPPORT_MULTIAUDIO")) {
+        cppflags = append(cppflags,"-DSUPPORT_MULTIAUDIO=1")
+    }
+
+    return cppflags
+}
diff --git a/services/audioflinger/Android.bp b/services/audioflinger/Android.bp
index 763c0702e1..3b128d64b6 100644
--- a/services/audioflinger/Android.bp
+++ b/services/audioflinger/Android.bp
@@ -1,4 +1,23 @@
+bootstrap_go_package {
+    name: "soong-audioflinger",
+    pkgPath: "android/soong/audioflinger",
+    deps: [
+        "blueprint",
+        "blueprint-pathtools",
+        "soong",
+        "soong-android",
+        "soong-cc",
+        "soong-genrule",
+    ],
+    srcs: [
+          "Android.go",
+    ],
+    pluginFor: ["soong_build"],
+}
 
+cc_audioflinger {
+   name: "cc_audioflinger_defaults",
+}
 
 package {
     default_applicable_licenses: [
@@ -21,7 +40,7 @@ license {
 
 cc_library_shared {
     name: "libaudioflinger",
-
+    defaults: ["cc_audioflinger_defaults"],
     srcs: [
         "AudioFlinger.cpp",
         "AudioHwDevice.cpp",
diff --git a/services/audioflinger/Android.go b/services/audioflinger/Android.go
new file mode 100755
index 0000000000..aa678c747b
--- /dev/null
+++ b/services/audioflinger/Android.go
@@ -0,0 +1,39 @@
+package audioflinger
+
+import (
+        "android/soong/android"
+        "android/soong/cc"
+        "fmt"
+)
+
+func init() {
+    fmt.Println("audioflinger want to conditional Compile")
+    android.RegisterModuleType("cc_audioflinger", DefaultsFactory)
+}
+
+func DefaultsFactory() (android.Module) {
+    module := cc.DefaultsFactory()
+    android.AddLoadHook(module, Defaults)
+    return module
+}
+
+func Defaults(ctx android.LoadHookContext) {
+    type props struct {
+        Cflags []string
+        Shared_libs []string
+    }
+
+    p := &props{}
+    p.Cflags = getCflags(ctx)
+    ctx.AppendProperties(p)
+}
+
+func getCflags(ctx android.BaseContext) ([]string) {
+    var cppflags []string
+
+    if (ctx.AConfig().IsEnvTrue("BOARD_SUPPORT_MULTIAUDIO")) {
+        cppflags = append(cppflags,"-DSUPPORT_MULTIAUDIO=1")
+    }
+
+    return cppflags
+}
diff --git a/services/audiopolicy/managerdefault/Android.bp b/services/audiopolicy/managerdefault/Android.bp
index 4b4817ec6e..271ddff08d 100644
--- a/services/audiopolicy/managerdefault/Android.bp
+++ b/services/audiopolicy/managerdefault/Android.bp
@@ -1,3 +1,24 @@
+bootstrap_go_package {
+    name: "soong-managerdefault",
+    pkgPath: "android/soong/managerdefault",
+    deps: [
+        "blueprint",
+        "blueprint-pathtools",
+        "soong",
+        "soong-android",
+        "soong-cc",
+        "soong-genrule",
+    ],
+    srcs: [
+          "Android.go",
+    ],
+    pluginFor: ["soong_build"],
+}
+
+cc_managerdefault {
+   name: "cc_managerdefault_defaults",
+}
+
 package {
     // See: http://go/android-license-faq
     // A large-scale-change added 'default_applicable_licenses' to import
@@ -9,7 +30,7 @@ package {
 
 cc_library_shared {
     name: "libaudiopolicymanagerdefault",
-
+    defaults: ["cc_managerdefault_defaults"],
     srcs: [
         "AudioPolicyManager.cpp",
         "EngineLibrary.cpp",
diff --git a/services/audiopolicy/managerdefault/Android.go b/services/audiopolicy/managerdefault/Android.go
new file mode 100755
index 0000000000..b75b7ebe22
--- /dev/null
+++ b/services/audiopolicy/managerdefault/Android.go
@@ -0,0 +1,39 @@
+package managerdefault
+
+import (
+        "android/soong/android"
+        "android/soong/cc"
+        "fmt"
+)
+
+func init() {
+    fmt.Println("managerdefault want to conditional Compile")
+    android.RegisterModuleType("cc_managerdefault", DefaultsFactory)
+}
+
+func DefaultsFactory() (android.Module) {
+    module := cc.DefaultsFactory()
+    android.AddLoadHook(module, Defaults)
+    return module
+}
+
+func Defaults(ctx android.LoadHookContext) {
+    type props struct {
+        Cflags []string
+        Shared_libs []string
+    }
+
+    p := &props{}
+    p.Cflags = getCflags(ctx)
+    ctx.AppendProperties(p)
+}
+
+func getCflags(ctx android.BaseContext) ([]string) {
+    var cppflags []string
+
+    if (ctx.AConfig().IsEnvTrue("BOARD_SUPPORT_MULTIAUDIO")) {
+        cppflags = append(cppflags,"-DSUPPORT_MULTIAUDIO=1")
+    }
+
+    return cppflags
+}
